#ifndef HASH_TEXT_SEARCH_H
#define HASH_TEXT_SEARCH_H

#include <string>
#include <vector>

std::vector<std::vector<int>>
find_hash(const std::string &text, const std::vector<std::string> &queries);

#endif // HASH_TEXT_SEARCH_H
