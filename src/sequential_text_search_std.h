#ifndef SEQUENTIAL_TEXT_SEARCH_STD_H
#define SEQUENTIAL_TEXT_SEARCH_STD_H

#include <string>
#include <vector>

std::vector<std::vector<int>> find_sequential_std(const std::string &text, const std::vector<std::string> &queries);

#endif
