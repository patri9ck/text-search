#pragma once
#include <string>
#include <vector>


std::vector<std::vector<int>> testCandidate_cl(const std::string &text,
                                               const std::vector<std::string> &queries);